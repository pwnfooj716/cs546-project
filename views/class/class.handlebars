{{#if isStudent}}
    <div id="student">
        <h1>{{class.name}}</h1>
        <ul id="assignment-list">
            {{#each assignments}}
                <li class="assignment"><a href="/class/{{../class.id}}/{{id}}">{{assignmentName}} {{dueDate}} {{grade}}</a></li>
            {{/each}}
        </ul>
        <a href="/class">back</a>
    </div>
{{else}}
    <div id="teacher">
    <table>
        <tr>
            <th></th>
            {{#each assignments}}
                <th>{{name}}</th>
            {{/each}}
        </tr>
        {{#each students}}
            <tr>
                <td>{{name}}</td>
                {{#each assignments}}
                    <td>{{this}} <a href="/class/{{../../class.id}}/{{@index}}/{{../_id}}">*</a></td>
                {{/each}}
            </tr>
        {{/each}}
    </table>
        <a href="/class/{{class.id}}/new">Create an assignment</a>
    </div>
    <a href="/class">back</a>
{{/if}}
<div id="announcements">
    {{#each annoucements}}
        <div>
            <h1>name</h1>
            <p>description</p>
        </div>
    {{/each}}
</div>
